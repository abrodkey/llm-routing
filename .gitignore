# Local-only cached pulls (regenerated by CI). Keep models.json + data/radar.json in git.
data/aa.json
data/epoch.csv
data/openrouter.json
data/or_endpoints/
data/arena_*.json
data/arena_*.parquet
data/arena_full/
data/_ep_test.json

# Python
__pycache__/
*.pyc
.venv/

# OS
.DS_Store
data/openrouter_usage.json
data/vectara.md

# Local-only dev config (preview server, IDE settings) — not for the repo
.claude/
